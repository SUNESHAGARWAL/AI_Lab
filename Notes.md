Deferred: token-by-token answer streaming. Current contract streams node-level events; generator answer arrives as one event on completion. True token streaming needs its own ADR resolving interactions with the gateway's semantic cache (cached responses have no stream), retry logic (partial-then-retry), and budget guard (mid-stream cutoff). Decide after the frontend MVP is working on node-level streaming.
Demo question (headline): "What specific fines can be imposed on a deployer of a high-risk AI system that fails to implement human oversight, and how do those penalties compare to GDPR fines?" → system correctly abstains with precise reasoning (identifies Art 99/100/Recital 91 as related-but-insufficient, declines to fabricate). This is the core value demo. Retry loop (RetryLoopEvent) is rare in practice because the generator abstains rather than overreaching — build the backward-edge animation but treat the abstention→HITL path as the visual centerpiece.

Confirmed: RetryLoopEvent is rare-to-never in practice. Two adversarial retry_budget-2 questions both abstained (graph_interrupted) rather than triggering a retry — generator abstains rather than overreaching, so the critic rarely has an unfaithful draft to reject. Graph animation centerpiece = abstention→HITL path (use #7A6E52), not the backward retry edge. Retry edge still built as safety-net visual but understated. Candidate ADR: "generator abstention is the primary faithfulness mechanism; critic retry is the edge-case safety net."
