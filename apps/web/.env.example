# The FastAPI backend's own origin — see lib/sse-client.ts.
# Local dev (matches `just dev`'s uvicorn port): http://localhost:8000
# Production (Vercel): your deployed Railway domain, e.g. https://your-api.up.railway.app
NEXT_PUBLIC_API_URL=http://localhost:8000
